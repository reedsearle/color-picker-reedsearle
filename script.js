window.onload = function () {  initColorPicker();}function initColorPicker() {  let colorbox = document.getElementById("color-box");  let rbg = {    red: document.getElementById("red"),    green: document.getElementById("green"),    blue: document.getElementById("blue"),  };  let colorPickers =      document.getElementsByClassName("picker");  setColorPickerEventListener(colorbox, rbg, colorPickers);}function setColorPickerEventListener(element, colors, pickerElements) {  let pickerLength = pickerElements.length;  for(let i = 0; i < pickerLength; i++ ) {    pickerElements[i].addEventListener("change", () => {      console.log("Change: ", pickerElements[i].id.toString(), "to: ",pickerElements[i].value);      let red = colors.red.value;      let green = colors.green.value;      let blue = colors.blue.value;      setElementBGColor(element, red, green, blue);      setDisplayValue(red, green, blue);    })  }}function setElementBGColor(colorbox, red, green, blue) {  let rgbval = [red, green, blue].join(',');  colorbox.style.backgroundColor = "rgb(" + rgbval + ")";}function setDisplayValue(red, green, blue) {  let redValue = document.getElementById("red-value");  let greenValue = document.getElementById("green-value");  let blueValue = document.getElementById("blue-value");  redValue.innerText = red;  greenValue.innerText = green;  blueValue.innerText = blue;}